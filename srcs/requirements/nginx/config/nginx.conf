worker_processes	auto;

events {
	worker_connections	128;
}

http {

	include			mime.types;
	default_type	application/octet-stream;
	sendfile		on;
	server_tokens	off;
	keepalive_timeout 60;

	# per-site server blocks (envsubst-rendered at container start)
	include			conf.d/*.conf;
}
